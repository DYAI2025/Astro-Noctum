node_modules/
build/
dist/
coverage/
__pycache__/
.DS_Store
*.log
*.crdownload
.env*
!.env.example

# Claude internal files
.claude/*
!.claude/commands/
!.claude/commands/*.md
!.claude/skills/
!.claude/skills/*/
!.claude/skills/*/*.md
# Project-wide hooks + deny rules — ship to all developers.
# settings.local.json (personal allow-rules, absolute paths) stays ignored.
!.claude/settings.json
.mcp.json

# Worktrees
.worktrees/
.worktrees/

# Local-only artefacts (prototype references + session kickoff docs)
Cymantics/
H_SESSION_KICKOFF.md

# Local tooling state (claude-flow swarm coordination, ruflo MCP memory DB)
.swarm/
ruvector.db

# Aphorism curator backup zips — the content is already tracked as
# individual aph-NNNN.md files under the same review/ folder; the zips
# are local-only redundant archives.
**/aphorisms/review/Archiv.zip
