# API-ONB-CHART-001: calculateAll() auf single /chart-Call umstellen

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** `calculateAll()` in `src/services/api.ts` macht genau einen POST `/calculate/chart`-Call und wirft bei jedem Fehler eine Exception — keine stillen Partial-Results mehr in Supabase.

**Architecture:** Der bestehende `postCalculation()`-Helper routet bereits korrekt nach `/api/calculate/chart` → BAFE `/calculate/chart`. Die Mappinglogik aus den fünf Einzelfunktionen wird in eine reine `mapChartToApiResults()`-Funktion konsolidiert. `calculateAll()` selbst wird auf drei Zeilen reduziert: validate → fetch → map. `MOCK_DATA` und `withFallback` werden entfernt.

**Tech Stack:** TypeScript, Vitest, `src/services/api.ts`, `src/types/bafe.ts`, `src/__tests__/api.test.ts` (neu).

---

### Task 1: ChartResponse-Typ in bafe.ts definieren

**Files:**
- Modify: `src/types/bafe.ts` — append nach `BafeTstResponse`

**Step 1: Typ appenden**

Füge direkt nach der `BafeTstResponse`-Definition ein:

```typescript
/** Unified response from BAFE /calculate/chart — all sub-domains in one payload. */
export interface ChartResponse {
  bazi?: BafeBaziResponse;
  /** Western planet bodies — /chart uses "positions", individual /western uses "bodies". */
  positions?: Record<string, BafeWesternBody>;
  bodies?: Record<string, BafeWesternBody>;   // fallback alias
  angles?: { Ascendant?: number; MC?: number; [key: string]: number | undefined };
  houses?: Record<string, number | string>;
  wuxing?: BafeWuxingResponse;
  fusion?: BafeFusionResponse;
  /** TST data — /chart nests this under time_scales. */
  time_scales?: BafeTstResponse;
  [key: string]: unknown;
}
```

**Step 2: Typecheck**

```bash
npm run lint
```

Expected: 0 errors.

**Step 3: Commit**

```bash
git add src/types/bafe.ts
git commit -m "feat(api-onb-chart-001): add ChartResponse type to bafe.ts"
```

---

### Task 2: mapChartToApiResults()-Funktion schreiben (TDD)

**Files:**
- Create: `src/__tests__/api.test.ts`
- Modify: `src/services/api.ts` — neue Funktion, noch nicht in calculateAll eingebaut

**Step 1: Failing tests schreiben**

Erstelle `src/__tests__/api.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock supabase before importing api.ts
vi.mock('../lib/supabase', () => ({
  supabase: {
    auth: {
      getSession: vi.fn().mockResolvedValue({ data: { session: null } }),
      refreshSession: vi.fn().mockResolvedValue({ data: {}, error: null }),
    },
  },
}));

// Import the internal mapper — it will be exported as named export
import { mapChartToApiResults } from '../services/api';
import type { ChartResponse } from '../types/bafe';

const MINIMAL_CHART: ChartResponse = {
  bazi: {
    pillars: {
      year:  { stamm: 'Geng', zweig: 'Zi', tier: 'Ratte',  element: 'Metall' },
      month: { stamm: 'Bing', zweig: 'Yin', tier: 'Tiger', element: 'Feuer'  },
      day:   { stamm: 'Jia', zweig: 'Wu',   tier: 'Pferd', element: 'Holz'   },
      hour:  { stamm: 'Wu',  zweig: 'Xu',   tier: 'Hund',  element: 'Erde'   },
    },
    chinese: { day_master: 'Jia', year: { animal: 'Ratte' } },
  },
  positions: {
    Sun:  { zodiac_sign: 9 },  // Capricorn
    Moon: { zodiac_sign: 3 },  // Cancer
  },
  angles: { Ascendant: 180 },  // Libra (180° = start of Libra)
  houses: { '1': 180, '2': 210, '3': 240, '4': 270, '5': 300, '6': 330,
            '7': 0,   '8': 30,  '9': 60,  '10': 90, '11': 120, '12': 150 },
  wuxing: {
    wu_xing_vector: { Holz: 2, Feuer: 1, Erde: 3, Metall: 2, Wasser: 2 },
    dominant_element: 'Earth',
  },
  fusion: { theme: 'Balance', summary: 'Test fusion' },
  time_scales: { scale: 'test' },
};

describe('mapChartToApiResults', () => {
  it('maps bazi pillars from German keys', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.bazi.pillars?.year.stem).toBe('Geng');
    expect(result.bazi.pillars?.year.branch).toBe('Zi');
    expect(result.bazi.pillars?.year.animal).toBe('Ratte');
  });

  it('extracts day_master and zodiac_sign from chinese field', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.bazi.day_master).toBe('Jia');
    expect(result.bazi.zodiac_sign).toBe('Ratte');
  });

  it('maps Sun to Capricorn (index 9)', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.western.zodiac_sign).toBe('Capricorn');
  });

  it('maps Moon to Cancer (index 3)', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.western.moon_sign).toBe('Cancer');
  });

  it('maps Ascendant degrees to sign name', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.western.ascendant_sign).toBe('Libra');
  });

  it('converts house cusp degrees to sign names', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.western.houses['1']).toBe('Libra');
    expect(result.western.houses['4']).toBe('Capricorn');
  });

  it('maps wuxing with English and German element keys', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.wuxing.elements.Wood).toBe(2);
    expect(result.wuxing.elements.Holz).toBe(2);
    expect(result.wuxing.dominant_element).toBe('Earth');
  });

  it('passes fusion through unchanged', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect(result.fusion.theme).toBe('Balance');
  });

  it('maps time_scales to tst', () => {
    const result = mapChartToApiResults(MINIMAL_CHART);
    expect((result.tst as Record<string, unknown>).scale).toBe('test');
  });

  it('falls back to bodies when positions is absent', () => {
    const chart: ChartResponse = {
      ...MINIMAL_CHART,
      positions: undefined,
      bodies: MINIMAL_CHART.positions,
    };
    const result = mapChartToApiResults(chart);
    expect(result.western.zodiac_sign).toBe('Capricorn');
  });

  it('throws when bazi.pillars is missing', () => {
    const chart: ChartResponse = { ...MINIMAL_CHART, bazi: {} };
    expect(() => mapChartToApiResults(chart)).toThrow('/chart response missing bazi.pillars');
  });

  it('throws when both positions and bodies are absent', () => {
    const chart: ChartResponse = { ...MINIMAL_CHART, positions: undefined, bodies: undefined };
    expect(() => mapChartToApiResults(chart)).toThrow('/chart response missing positions');
  });

  it('throws when wuxing is absent', () => {
    const chart: ChartResponse = { ...MINIMAL_CHART, wuxing: undefined };
    expect(() => mapChartToApiResults(chart)).toThrow('/chart response missing wuxing');
  });
});
```

**Step 2: Test laufen lassen — muss FAIL mit "mapChartToApiResults is not exported"**

```bash
npx vitest run src/__tests__/api.test.ts 2>&1 | tail -20
```

Expected: FAIL.

**Step 3: mapChartToApiResults() in api.ts implementieren**

Füge direkt nach den bestehenden `calculate*`-Funktionen (nach Zeile ~298, vor `MOCK_DATA`) ein:

```typescript
// ── mapChartToApiResults ────────────────────────────────────────────
// Pure function: ChartResponse → { bazi, western, wuxing, fusion, tst }
// Throws if any required section is absent — no silent partial results.

export function mapChartToApiResults(raw: ChartResponse): Omit<ApiResults, 'issues' | '_reading_id'> {
  if (!raw.bazi?.pillars) {
    throw new Error('/chart response missing bazi.pillars');
  }
  const bodiesSource = raw.positions || raw.bodies;
  if (!bodiesSource) {
    throw new Error('/chart response missing positions/bodies');
  }
  if (!raw.wuxing) {
    throw new Error('/chart response missing wuxing');
  }

  // ── BaZi mapping (same logic as calculateBazi) ──────────────────
  const mapPillar = (p: BafePillarRaw | undefined): MappedPillar => ({
    stem:    p?.stamm   || p?.stem   || '',
    branch:  p?.zweig   || p?.branch || '',
    animal:  p?.tier    || p?.animal || '',
    element: p?.element || '',
  });

  const bazi: MappedBazi = {
    ...raw.bazi,
    pillars: {
      year:  mapPillar(raw.bazi.pillars.year),
      month: mapPillar(raw.bazi.pillars.month),
      day:   mapPillar(raw.bazi.pillars.day),
      hour:  mapPillar(raw.bazi.pillars.hour),
    },
    day_master: raw.bazi.chinese?.day_master || raw.bazi.pillars.day?.stamm || '',
    zodiac_sign: raw.bazi.chinese?.year?.animal || raw.bazi.pillars.year?.tier || '',
  };

  // ── Western mapping (same logic as calculateWestern) ────────────
  const sunSign        = signFromIndex(bodiesSource?.Sun?.zodiac_sign);
  const moonSign       = signFromIndex(bodiesSource?.Moon?.zodiac_sign);
  const ascendantSign  = signFromDegrees(raw.angles?.Ascendant);

  const normalizedHouses: Record<string, string> = {};
  if (raw.houses && typeof raw.houses === 'object') {
    Object.entries(raw.houses).forEach(([key, deg]) => {
      if (typeof deg === 'number') {
        normalizedHouses[key] = signFromDegrees(deg) || '';
      } else if (typeof deg === 'string') {
        normalizedHouses[key] = deg;
      }
    });
  }

  const western: MappedWestern = {
    ...raw,
    bodies: bodiesSource,
    angles: raw.angles,
    zodiac_sign:    sunSign,
    moon_sign:      moonSign,
    ascendant_sign: ascendantSign,
    houses:         normalizedHouses,
  };

  // ── Wu-Xing mapping (same logic as calculateWuxing) ─────────────
  const vec = raw.wuxing.wu_xing_vector || {};
  const wuxing: MappedWuxing = {
    ...raw.wuxing,
    elements: {
      Wood:   vec.Holz   ?? vec.Wood   ?? 0,
      Fire:   vec.Feuer  ?? vec.Fire   ?? 0,
      Earth:  vec.Erde   ?? vec.Earth  ?? 0,
      Metal:  vec.Metall ?? vec.Metal  ?? 0,
      Water:  vec.Wasser ?? vec.Water  ?? 0,
      Holz:   vec.Holz   ?? vec.Wood   ?? 0,
      Feuer:  vec.Feuer  ?? vec.Fire   ?? 0,
      Erde:   vec.Erde   ?? vec.Earth  ?? 0,
      Metall: vec.Metall ?? vec.Metal  ?? 0,
      Wasser: vec.Wasser ?? vec.Water  ?? 0,
    },
    dominant_element: raw.wuxing.dominant_element || '',
  };

  // ── Fusion — pass through from chart payload ─────────────────────
  const fusion: BafeFusionResponse = raw.fusion || {};

  // ── TST from time_scales ─────────────────────────────────────────
  const tst: BafeTstResponse = raw.time_scales || {};

  return { bazi, western, wuxing, fusion, tst };
}
```

Add the import for `ChartResponse` at the top of `api.ts`:

```typescript
import type {
  // ...existing imports...
  ChartResponse,       // ← add this
} from '../types/bafe';
```

**Step 4: Tests laufen lassen — müssen PASS**

```bash
npx vitest run src/__tests__/api.test.ts 2>&1 | tail -20
```

Expected: All 12 tests PASS.

**Step 5: Commit**

```bash
git add src/services/api.ts src/__tests__/api.test.ts
git commit -m "feat(api-onb-chart-001): add mapChartToApiResults() with full test coverage"
```

---

### Task 3: calculateAll() auf single /chart-Call umstellen

**Files:**
- Modify: `src/services/api.ts` — `calculateAll()` ersetzen, `MOCK_DATA` + `withFallback` entfernen

**Step 1: calculateAll() failure-path Test hinzufügen**

Ergänze in `src/__tests__/api.test.ts` eine neue describe-Suite:

```typescript
import { calculateAll } from '../services/api';

// Mock postCalculation via fetch mock
describe('calculateAll', () => {
  it('throws when /chart returns 500', async () => {
    const { supabase } = await import('../lib/supabase');
    vi.mocked(supabase.auth.getSession).mockResolvedValue({
      data: { session: { access_token: 'tok' } as any },
      error: null,
    });

    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: false,
      status: 500,
      statusText: 'Internal Server Error',
      text: async () => 'Server Error',
    }));

    await expect(calculateAll({
      date: '1990-01-15T12:00:00',
      tz: 'Europe/Berlin',
      lon: 13.4,
      lat: 52.5,
    })).rejects.toThrow();

    vi.unstubAllGlobals();
  });

  it('returns issues: [] on success', async () => {
    const { supabase } = await import('../lib/supabase');
    vi.mocked(supabase.auth.getSession).mockResolvedValue({
      data: { session: null },
      error: null,
    });

    const chartPayload: ChartResponse = MINIMAL_CHART;
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: true,
      status: 200,
      json: async () => chartPayload,
    }));

    const result = await calculateAll({
      date: '1990-01-15T12:00:00',
      tz: 'Europe/Berlin',
      lon: 13.4,
      lat: 52.5,
    });

    expect(result.issues).toEqual([]);
    expect(result.bazi.day_master).toBe('Jia');
    vi.unstubAllGlobals();
  });
});
```

**Step 2: Test laufen lassen — FAIL erwartet (calculateAll gibt noch keine Exception bei 500)**

```bash
npx vitest run src/__tests__/api.test.ts 2>&1 | tail -20
```

**Step 3: calculateAll() umschreiben**

Ersetze die bestehende `calculateAll()`-Funktion und entferne `MOCK_DATA` vollständig:

```typescript
export async function calculateAll(data: BirthData): Promise<ApiResults> {
  validateBirthData(data);

  // Refresh token before the chart call — new users may have stale token
  // after spending time on the birth form.
  try {
    await supabase.auth.refreshSession();
  } catch (refreshErr) {
    console.warn('[api] Session refresh failed, proceeding with cached token:', refreshErr);
  }

  const raw = await postCalculation<ChartResponse>('chart', {
    date: data.date,
    tz: data.tz,
    lon: data.lon,
    lat: data.lat,
    ambiguousTime: 'earlier',
    nonexistentTime: 'error',
  });

  const mapped = mapChartToApiResults(raw);
  return { ...mapped, issues: [] };
}
```

Lösche die `MOCK_DATA`-Konstante (Zeilen 301–326 in der originalen Datei) und die `withFallback`-Helper-Funktion.

**Step 4: Tests laufen lassen — alle müssen PASS**

```bash
npx vitest run src/__tests__/api.test.ts 2>&1 | tail -20
```

Expected: All PASS.

**Step 5: TypeScript clean**

```bash
npm run lint 2>&1 | grep "error TS"
```

Expected: 0 errors.

**Step 6: Gesamte Test-Suite**

```bash
npm run test 2>&1 | tail -15
```

Expected: No new failures (pre-existing flaky performance test ist OK).

**Step 7: Commit**

```bash
git add src/services/api.ts src/__tests__/api.test.ts
git commit -m "feat(api-onb-chart-001): calculateAll() uses single /chart call — no partial results"
```

---

### Task 4: Konsumenten-Check und deprecated markieren

**Files:**
- Modify: `src/services/api.ts` — JSDoc auf unused Einzelfunktionen

**Step 1: Konsumenten prüfen**

```bash
grep -rn "calculateBazi\|calculateWestern\|calculateFusion\|calculateWuxing\|calculateTst" src/ --include="*.ts" --include="*.tsx" | grep -v "api.ts\|__tests__"
```

Expected: Keine Treffer (alle Einzelfunktionen werden nur intern in api.ts verwendet).

**Step 2: Falls keine Konsumenten — JSDoc-Deprecation ergänzen**

Füge `@deprecated` JSDoc über jede der fünf Einzelfunktionen in `api.ts` ein:

```typescript
/**
 * @deprecated Since API-ONB-CHART-001 — use calculateAll() which calls /chart atomically.
 * Kept for potential direct use; remove once confirmed unused.
 */
export async function calculateBazi(data: BirthData): Promise<MappedBazi> {
```

Gleich für `calculateWestern`, `calculateFusion`, `calculateWuxing`, `calculateTst`.

**Step 3: Typecheck**

```bash
npm run lint 2>&1 | grep "error TS"
```

Expected: 0 errors.

**Step 4: Commit**

```bash
git add src/services/api.ts
git commit -m "chore(api-onb-chart-001): mark individual calculate* functions as deprecated"
```

---

---

## Verifikation

### Automatisiert (nach allen Tasks zwingend)

```bash
npm run test 2>&1 | tail -10      # alle Tests grün
npm run lint 2>&1 | grep "error"  # 0 TypeScript errors
```

### No-Partial-Write Garant (strenge Verifikation)

Dieser Test stellt sicher dass `upsertAstroProfile` bei einem `/chart`-Fehler **niemals** aufgerufen wird.
Füge ihn in `src/__tests__/api.test.ts` als eigene describe-Suite ein — er muss PASS bevor du shippen kannst:

```typescript
import { vi } from 'vitest';
import * as supabaseService from '../services/supabase';

describe('no-partial-write guarantee', () => {
  it('upsertAstroProfile is never called when calculateAll throws', async () => {
    // Arrange: /chart returns 500 → calculateAll throws
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
      ok: false,
      status: 500,
      statusText: 'Internal Server Error',
      text: async () => 'Server Error',
    }));
    const upsertSpy = vi.spyOn(supabaseService, 'upsertAstroProfile');

    // Act: simulate what useAstroProfile.handleSubmit does
    let errorCaught = false;
    try {
      const results = await calculateAll({
        date: '1990-01-15T12:00:00',
        tz: 'Europe/Berlin',
        lon: 13.4,
        lat: 52.5,
      });
      // If calculateAll didn't throw, simulate the upsert call
      await supabaseService.upsertAstroProfile(
        'user-123', { date: '1990-01-15T12:00:00', tz: 'Europe/Berlin', lon: 13.4, lat: 52.5 },
        results as any, 'interpretation', {}
      );
    } catch {
      errorCaught = true;
    }

    // Assert
    expect(errorCaught).toBe(true);
    expect(upsertSpy).not.toHaveBeenCalled();

    vi.unstubAllGlobals();
    vi.restoreAllMocks();
  });
});
```

Run:
```bash
npx vitest run src/__tests__/api.test.ts --reporter=verbose 2>&1 | grep -E "PASS|FAIL|✓|✗|no-partial"
```

Expected: `no-partial-write guarantee > upsertAstroProfile is never called when calculateAll throws ✓`

### Manual check (nach Deployment)

1. Neuen Testuser registrieren
2. Onboarding mit gültigen Geburtsdaten durchlaufen
3. Supabase → `astro_profiles` → `astro_json`: `bazi.pillars`, `western.zodiac_sign`, `wuxing.dominant_element` alle vorhanden, keins leer
4. Netzwerk-Tab: genau **ein** POST `/api/calculate/chart` — nicht 5 Parallel-Calls

## Risiken

| Risiko | Mitigation |
|--------|-----------|
| `/calculate/chart` existiert auf BAFE nicht | Wird sofort als 404 sichtbar — kein stilles Fallback mehr. Prüf zuerst mit `curl` gegen staging BAFE |
| fusion im /chart-Payload hat andere Struktur als /fusion | mapChartToApiResults nimmt `raw.fusion || {}` — Dashboard-Komponenten nutzen `fusion?.theme` (optional chain) |
| Bestehende Testuser haben Altprofile mit Partial-Data | Out of Scope — nur Neuregistrierungen betroffen |
